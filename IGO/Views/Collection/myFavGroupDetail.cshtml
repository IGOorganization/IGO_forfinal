@model IEnumerable<IGO.Models.TCollectionGroupDetail>

@{
    ViewData["Title"] = "myFavGroupDetail";
}

<br />
<h1>群組明細</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@

@*<table class="table">
    <thead>
        <tr>
            <th hidden>
                @Html.DisplayNameFor(model => model.FCollectionGroupDetailId)
            </th>
            <th>
                群組ID
            </th>
            <th>
                收藏ID
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td hidden>
                    @Html.DisplayFor(modelItem => item.FCollectionGroupDetailId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FCollectionGroupId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FCollectionId)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>*@





@*Bottom*@
<div class="container newsletter mt-5 wow fadeIn" data-wow-delay="0.1s" id="full-1">
    <div class="row justify-content-center">
        <div @*class="col-lg-10 border rounded p-1"*@>
            <div @*class="border rounded text-center p-1"*@>
                <div class="bg-transparent rounded text-center p-5">
                    <h4 class="mb-4">@*Subscribe Our*@ <span class="text-primary text-uppercase">@*Newsletter*@</span></h4>
                    <div @*class="position-relative mx-auto"*@ style="max-width: 400px;">
                        @*<input class="form-control w-100 py-3 ps-4 pe-5" type="text" placeholder="Enter your email">
                            <button type="button" class="btn btn-primary py-2 px-3 position-absolute top-0 end-0 mt-2 me-2">Submit</button>*@
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@*Bottom*@
